# Дорожная карта документов для строительного объекта

## Описание

Реализована система отслеживания статусов документов для строительного объекта на основе дорожной карты. Система позволяет управлять статусами выполнения работ, сроками действия документов, загружать файлы и получать автоматические уведомления.

## Установка и настройка

### 1. Инициализация структуры дорожной карты

Перед первым использованием необходимо инициализировать структуру дорожной карты в базе данных:

```bash
cd backend
python init_roadmap_sections.py
```

Это создаст все узлы дорожной карты согласно схеме:
- Эскизный проект
  - Инженерно-технические условия (с подразделами)
  - Инженерно-геологические условия
  - Градостроительное заключение
- Рабочий проект
  - Стройгенплан
  - ППР
  - Акт выноса в натуру
  - ГП АР (с согласованиями)
  - Прохождение госэкспертизы (с этапами)
  - Проекты Инженерные сети (наружные и внутренние)
  - Включение в реестр

### 2. Создание директории для файлов

Система сохраняет загруженные PDF файлы в директорию `backend/uploads/documents/`. Убедитесь, что директория существует и доступна для записи:

```bash
mkdir -p backend/uploads/documents
```

## Использование

### Frontend

1. Откройте страницу "Дашборд управления строительством"
2. Выберите проект из выпадающего списка
3. Отобразится древовидная структура дорожной карты документов
4. Для каждого узла можно:
   - Добавить/изменить статус (кнопка "Добавить" / "Изменить")
   - Загрузить PDF файлы (кнопка "Файлы")
   - Просмотреть цветовые индикаторы статусов

### API Endpoints

#### Получить все секции дорожной карты
```
GET /api/v1/document-roadmap/sections/
```

#### Получить статусы для проекта
```
GET /api/v1/document-roadmap/statuses/?project_id={project_id}
```

#### Создать статус узла
```
POST /api/v1/document-roadmap/statuses/
Body: {
  "project_id": 1,
  "section_code": "sketch.itc.heat",
  "request_date": "2024-01-15",
  "due_date": "2024-02-15",
  "valid_until_date": "2025-01-15",
  "executor_company": "Иванов И.И.",
  "executor_authority": "Петров П.П.",
  "execution_status": "in_progress",
  "note": "Примечание"
}
```

#### Обновить статус
```
PUT /api/v1/document-roadmap/statuses/{status_id}
Body: { ... }
```

#### Загрузить файл
```
POST /api/v1/document-roadmap/statuses/{status_id}/files
Content-Type: multipart/form-data
Body: file (PDF)
```

#### Получить список файлов
```
GET /api/v1/document-roadmap/statuses/{status_id}/files
```

#### Скачать файл
```
GET /api/v1/document-roadmap/files/{file_id}/download
```

## Цветовые индикаторы

### Статус выполнения:
- 🔴 **Красный** - "Не начат"
- 🟡 **Желтый** - "В работе"
- 🟢 **Зеленый** - "Выполнено"
- 🔵 **Синий** - "На согласовании"

### Статус документа (по сроку действия):
- 🟢 **Зеленый** - Действителен (>30 дней до истечения)
- 🟡 **Желтый** - Скоро истекает (7-30 дней до истечения)
- 🔴 **Красный** - Просрочен (срок истек)

## Система уведомлений

Система автоматически создает уведомления в следующих случаях:

1. **При изменении статуса на "Выполнено"**
   - Текст: "Исправлен статус по разделу '[Название раздела]'"

2. **За 30 дней до истечения срока действия документа**
   - Текст: "Внимание! До истечения срока действия документа по разделу '[Название раздела]' осталось 30 дней. Дата истечения: [дата]"

3. **За 7 дней до истечения срока**
   - Текст: "СРОЧНО! До истечения срока действия документа по разделу '[Название раздела]' осталось 7 дней. Дата истечения: [дата]"

4. **При истечении срока**
   - Текст: "ПРОСРОЧЕНО! Срок действия документа по разделу '[Название раздела]' истек [дата]"

Уведомления сохраняются в таблице `document_notifications` и могут быть использованы для отображения в интерфейсе или отправки по email/push.

## Модели данных

### DocumentRoadmapSection
Статическая структура узлов дорожной карты.

### DocumentSectionStatus
Статус узла дорожной карты для конкретного проекта. Содержит все поля из ТЗ:
- Дата обращения
- Срок исполнения
- Срок действия документа
- Исполнитель от Авангарда
- Исполнитель от гос органа
- Статус выполнения
- Статус документа (автоматически рассчитывается)
- Примечание

### DocumentFile
Файл, прикрепленный к статусу узла. Поддерживается только PDF.

### DocumentNotification
Уведомление о событиях по дорожной карте документов.

## Примечания

- Для установки статуса "Выполнено" рекомендуется загрузить хотя бы один PDF файл (проверка может быть добавлена в бизнес-логику)
- Статус документа рассчитывается автоматически на основе поля `valid_until_date`
- Все даты хранятся в формате ISO (YYYY-MM-DD)
- Файлы сохраняются на сервере в директории `uploads/documents/`
