# Реализация светлой и темной темы

## ✅ Выполнено

### 1. Переключатель темы
- ✅ Добавлена кнопка переключения в topbar
- ✅ Иконка меняется в зависимости от темы (солнце/луна)
- ✅ Сохранение выбора в localStorage
- ✅ Автоматическое применение темы при загрузке

### 2. CSS переменные
- ✅ Темная тема (по умолчанию) - все переменные определены
- ✅ Светлая тема - все переменные переопределены через `[data-theme="light"]`

### 3. Стили для светлой темы
Реализованы стили для всех основных элементов:
- ✅ Body и фоновые градиенты
- ✅ Sidebar (навигация)
- ✅ Topbar (верхняя панель)
- ✅ Cards (карточки)
- ✅ Buttons (кнопки)
- ✅ Tables (таблицы)
- ✅ Inputs, selects, textareas (поля ввода)
- ✅ Chips (статусы)
- ✅ Tabs (вкладки)
- ✅ Stepper (этапы workflow)
- ✅ Nav links (ссылки навигации)
- ✅ Pills (пилюли статуса)
- ✅ KPI items
- ✅ Brand (логотип)

### 4. Переходы
- ✅ Плавные переходы (transition) для всех элементов
- ✅ Изменение темы происходит без мерцания

## 🎨 Цветовая схема

### Темная тема (по умолчанию)
- Фон: `#0b1020`
- Панели: `#0f1731`
- Карточки: `#111b3b`
- Текст: `#e9edff`
- Акцент: `#6ea8fe`

### Светлая тема
- Фон: `#f8f9fa`
- Панели: `#ffffff`
- Карточки: `#ffffff`
- Текст: `#1a1f2e`
- Акцент: `#0d6efd`

## 🔧 Использование

Тема применяется через атрибут `data-theme` на `<html>` элементе:
- `data-theme="dark"` - темная тема
- `data-theme="light"` - светлая тема

Переключение происходит через кнопку в topbar. Выбор пользователя сохраняется в localStorage и автоматически применяется при следующей загрузке.

## 📱 Адаптивность

Все стили тем работают на всех разрешениях экрана (mobile, tablet, desktop).
